global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
  - job_name: node-exporter
    static_configs:
      - targets: ["node-exporter:9100"]
  - job_name: cadvisor
    static_configs:
      - targets: ["cadvisor:8080"]
  - job_name: postgres-exporter
    static_configs:
      - targets: ["postgres-exporter:9187"]
  - job_name: pushgateway
    honor_labels: true
    static_configs:
      - targets: ["pushgateway:9091"]
  # Cloudflare Tunnel (cloudflared): run with --metrics <host>:<port> and set target below
  # - job_name: cloudflared
  #   static_configs:
  #     - targets: ["host.docker.internal:60123"]
  # Backend services (uncomment and set correct targets when services run; metrics_path: /v1/metrics)
  # - job_name: sso-service
  #   static_configs:
  #     - targets: ["host.docker.internal:4001"]
  #   metrics_path: /v1/metrics
